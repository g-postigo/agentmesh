# Local dev broker for agentmesh. No auth, no TLS. Not for production.
# For production, see docs/broker-production.md (lands with v0.5.0).

port: 4222
http_port: 8222
server_name: agentmesh-dev

jetstream {
  store_dir: /data
  max_memory_store: 256MB
  max_file_store: 1GB
}
